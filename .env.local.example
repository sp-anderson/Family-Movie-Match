# TMDB — themoviedb.org > Settings > API > "API Key (v3 auth)"
TMDB_API_KEY=

# Google OAuth — console.cloud.google.com > APIs & Services > Credentials
# Authorized redirect URI must be:
#   https://family-movie-match.vercel.app/api/auth/callback/google
#   http://localhost:3000/api/auth/callback/google   (for local dev)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# NextAuth — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://family-movie-match.vercel.app

# Vercel KV — created automatically when you add the "KV" storage
# integration to this project in the Vercel dashboard; it fills these in.
KV_REST_API_URL=
KV_REST_API_TOKEN=
